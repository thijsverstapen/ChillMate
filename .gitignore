# Xcode / build artifacts
DerivedDataMCP/
DerivedData/
build/
*.xcarchive
*.log

# Xcode user state
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Xcode Cloud workflows, written into the project by the IDE. The build lives in
# .github/workflows/ci.yml; a second, IDE-managed description of it would drift.
ChillMate.xcodeproj/xcshareddata/xcodecloud/

# macOS
.DS_Store

# StoreKit config lives in ChillMate/ChillMate.storekit; ignore a stray root copy
/ChillMate.storekit

# Swift Package Manager
.swiftpm/
.build/

# CocoaPods / Carthage (if ever used)
Pods/
Carthage/Build/

# Secrets / signing — never commit these
*.p8
*.mobileprovision
*.p12
*.entitlements.dump

# Device-build DerivedData
DerivedDataDevice/

# Stray shell-redirect artifacts (a file literally named "-" was committed once)
/-

# Python bytecode from the scripts/ checks
__pycache__/
*.pyc

# xcresult bundles written by the CI test steps into the workspace
TestResults/

# Python bytecode from tools/
__pycache__/
*.pyc

# Claude Code's local session state and per-machine settings
.claude/
